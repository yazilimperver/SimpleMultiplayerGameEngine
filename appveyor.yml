image:
- Visual Studio 2017

branches:
  only:
    - CI_InitialIntegration

install:
  - set QTDIR=C:\Qt\5.12.2\msvc2017
  - set PATH=%QTDIR%\bin;C:\Qt\Tools\mingw730\x86_64-w64-mingw32\bin;%PATH%;
  - set QtMsBuild=%appveyor_repo_branch%\QtMsBuild
